import { test, expect } from '@playwright/test'
test('landing and login routes are usable', async ({ page }) => { await page.goto('/'); await expect(page.getByRole('heading', { name: /Trace the work/i })).toBeVisible(); await page.screenshot({ path: 'test-results/landing.png', fullPage: true }); await page.getByRole('link', { name: /Enter workspace/i }).click(); await expect(page).toHaveURL(/\/login/); await expect(page.getByLabel('Email')).toBeVisible(); await expect(page.getByRole('button', { name: /Sign in/i })).toBeVisible() })
